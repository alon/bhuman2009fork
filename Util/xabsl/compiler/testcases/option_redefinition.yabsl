/* redefinition of option */
option alpha
{
	@gamma;

	initial state beta
	{
  		subsequent-option {
  			alpha(gamma = @gamma);
  		}

		decision-tree {
			transition-to-state(beta);
		}
		
	}
}

option alpha
{
	@gamma;

	initial state beta
	{
  		subsequent-option {
  			alpha(gamma = @gamma);
  		}

		decision-tree {
			transition-to-state(beta);
		}
		
	}
}